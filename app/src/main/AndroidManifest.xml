<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coursehelper">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:name=".ResApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CourseHelper"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MainActivity">
        </activity>
        <activity android:name=".ui.activity.RegisterActivity">
        </activity>
        <activity android:name=".ui.activity.AddCourseActivity">
        </activity>
        <activity android:name=".ui.activity.OptinalCourseInfoActivity">
        </activity>
        <activity android:name=".ui.activity.SelectCourseActivity">
        </activity>
    </application>

</manifest>